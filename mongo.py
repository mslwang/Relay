import schema as schema
from flask import Flask, request

app = Flask(__name__)

@app.route('/newSignup', methods = ['POST'])
def add():
    
@app.route('/retrieve', methods = ['GET'])
def retrieve()
    phone_number = json.loads


app = Flask(__name__)

if __name__ == "__main__":
    db = schema.initial()
    app.run(threaded=True, port=6000)